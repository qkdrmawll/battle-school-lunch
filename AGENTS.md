# AGENTS.md

## 프로젝트 개요

급식배틀은 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 분석하는
웹 애플리케이션을 단계적으로 구현하는 워크숍 프로젝트입니다. 현재 저장소에는
워크숍 문서와 API 명세가 있으며, 이후 Python 백엔드와 TypeScript 프론트엔드,
배포 자동화 및 에이전트 워크플로가 추가될 수 있습니다.

## 일반 작업 지침

- 요청된 범위와 관련 파일을 먼저 확인하고, 필요한 변경만 작게 수행합니다.
- 이 문서보다 더 구체적인 하위 디렉터리의 `AGENTS.md`, 프로젝트 설정,
  기존 코드와 문서의 규칙을 우선합니다.
- 기존 구조, 명명법, 공개 인터페이스 및 사용자 동작을 가능한 한 유지합니다.
- 비슷한 구현과 공용 유틸리티를 먼저 찾아 재사용합니다.
- 현재 요구사항에 필요하지 않은 추상화, 의존성, 기능 또는 대규모 리팩터링을
  추가하지 않습니다.
- 동작이 바뀌면 관련 테스트와 문서를 함께 수정합니다.
- 파일 형식은 `.editorconfig`를 따릅니다.

## Python 가이드라인

- 공개 함수, 메서드 및 주요 데이터 구조에는 구체적인 타입을 사용하고 타입
  안정성을 우회하는 광범위한 `Any` 사용을 피합니다.
- 입력과 외부 API 응답은 경계에서 검증하며, 실패를 성공 값이나 빈 값으로
  가장하지 않습니다.
- 예외는 처리할 수 있는 구체적인 범위에서만 포착하고, 원인과 문맥을 보존해
  호출자에게 전달하거나 프로젝트의 로깅 방식에 따라 보고합니다.
- 표준 라이브러리와 기존 의존성을 우선하며, 새 의존성은 명확한 필요가 있을
  때만 프로젝트의 의존성 파일과 잠금 파일에 함께 반영합니다.
- 작은 단일 책임 함수, 명확한 이름, 테스트 가능한 경계를 선호하며 중복
  구현을 만들지 않습니다.

## TypeScript 가이드라인

- `strict` 설정과 기존 컴파일러 규칙을 유지하고, 타입 오류를 `any`, 불필요한
  타입 단언 또는 검사 비활성화로 회피하지 않습니다.
- 외부 데이터와 사용자 입력은 런타임에 검증하고, 선택 값과 오류 상태를 타입에
  명시적으로 표현합니다.
- 비동기 오류를 누락하지 않으며, 사용자에게 필요한 오류는 기존 UI 패턴에 따라
  이해할 수 있게 표시합니다.
- 기존 컴포넌트, 훅, 타입과 유틸리티를 재사용하고 표현 계층과 데이터 접근
  로직의 책임을 분리합니다.
- 새 패키지는 기존 기능으로 해결할 수 없는 경우에만 추가하고 매니페스트와 잠금
  파일을 일관되게 갱신합니다.

## 테스트 및 유효성 검사

- 변경된 동작의 정상 경로, 실패 경로 및 중요한 경계 조건을 테스트합니다.
- 버그 수정에는 가능하면 수정 전 실패하고 수정 후 통과하는 회귀 테스트를
  추가합니다.
- 저장소에 실제로 존재하는 매니페스트, 스크립트, 설정 및 CI 워크플로를 확인한
  뒤 구성된 테스트, 포맷팅, 린트, 타입 검사와 빌드만 실행합니다.
- 아직 구성되지 않은 도구를 설치하거나 실행 명령을 임의로 만들지 않습니다.
- 가장 작은 관련 검증부터 실행하고, 공용 영역이나 설정에 영향을 주는 변경은
  필요한 범위까지 검증을 확대합니다.
- 실행할 수 없는 검증이 있다면 그 이유와 검증하지 못한 범위를 명확히 남깁니다.

## 보안

- 비밀, 자격 증명, 개인 정보 또는 실제 환경 값을 코드, 로그, 테스트 데이터와
  커밋에 포함하지 않습니다.
- 외부 입력을 신뢰하지 말고 검증하며, 출력 문맥에 맞는 안전한 처리 방식을
  사용합니다.
- 민감한 값은 환경 변수 또는 프로젝트에서 정한 비밀 관리 수단을 사용합니다.
- 새 의존성, 권한, 네트워크 접근은 최소화하고 보안 관련 실패를 숨기지 않습니다.
- 보안 취약점은 공개 이슈가 아니라 `SECURITY.md`의 절차를 따릅니다.

## 문서화

- 공개 API, 설정, 실행 방법 또는 사용자 동작이 바뀌면 관련 문서를 같은
  변경에서 갱신합니다.
- 문서는 현재 구현과 검증된 명령만 설명하고, 아직 존재하지 않는 기능을
  완료된 것처럼 작성하지 않습니다.
- 복잡한 결정은 무엇을 하는지보다 왜 필요한지를 코드 주석이나 관련 문서에
  간결하게 기록합니다.

## 주의사항 및 가드레일

- 사용자 변경이나 관련 없는 변경을 되돌리거나 덮어쓰지 않습니다.
- 생성 파일과 잠금 파일은 해당 도구를 통해 갱신하고 수동으로 임의 편집하지
  않습니다.
- 공개 API 또는 데이터 형식의 호환성을 깨는 변경은 명시적인 요구와 마이그레이션
  계획 없이 수행하지 않습니다.
- 오류를 무시하는 빈 예외 처리, 무조건 성공하는 대체 경로, 테스트 비활성화로
  검증을 통과시키지 않습니다.
- 삭제, 데이터 변경, 권한 확대와 같은 파괴적 작업은 범위를 확인하고 최소한으로
  수행합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 `CONTRIBUTING.md`의 Conventional
  Commits 규칙을 따릅니다.
- 커밋에 비밀, 임시 파일, 로컬 전용 설정 또는 관련 없는 변경을 포함하지
  않습니다.
- Pull Request를 작성할 때는 `.github/PULL_REQUEST_TEMPLATE.md`를 반드시
  활용해야 합니다. 템플릿의 항목을 생략하지 말고 변경 이유, 관련 이슈, 변경
  유형 및 실제 검증 내용을 빠짐없이 작성합니다.
- 관련 이슈는 `Closes #<번호>` 형식으로 연결하고, 리뷰 가능한 크기를
  유지합니다.
- 제출 전 diff를 자체 검토하고 관련 검사와 문서 갱신 여부를 확인합니다.
